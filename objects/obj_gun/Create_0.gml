/// @description
firing_delay=0;//make it so we don't fire th gun every single frame
recoil =0; //how far you want to move your gun back decreases back with each frame]
controller_angle=0;//angle aimed in with controller
if (global.hasgun==false) instance_destroy();